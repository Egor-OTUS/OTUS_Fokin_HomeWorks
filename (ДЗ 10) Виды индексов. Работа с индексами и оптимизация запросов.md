**Стек**  
Virtual Box 7.2.4      
Ubuntu Server 25.10  
Характеристики ВМ    
RAM: 16GB    
CPU: 8 cores  
VDI(SSD): 200GB  
PostgreSQL 17  

***Создать индекс к какой-либо из таблиц вашей БД***  
***Прислать текстом результат команды explain, в которой используется данный индекс*** 

Создаем БД и таблицу  
<img width="562" height="209" alt="image" src="https://github.com/user-attachments/assets/57526d38-7257-4e7f-a48b-da6786664f45" />

Наполняем её данными, т.к. оптимизатор будет использоваться лишь Seq Scan, поскольку в пустой таблице Seq Scan будет быстрее Index Scan  
<img width="790" height="193" alt="image" src="https://github.com/user-attachments/assets/bcd6aeef-2377-468a-9069-3f4f30dfad35" />

Используем explain до создания индекса  
<img width="818" height="153" alt="image" src="https://github.com/user-attachments/assets/accb8f04-92b1-4103-868a-081789af3ad1" />

Создаем индекс  
<img width="636" height="35" alt="image" src="https://github.com/user-attachments/assets/f6dcbf17-dfd4-420a-a633-667e6e83b890" />

Проверям создание индекса    
<img width="797" height="229" alt="image" src="https://github.com/user-attachments/assets/a722a5bb-263a-4dac-a1a7-b67f67ec3f71" />

Используем explain после создания индекса  
<img width="1055" height="132" alt="image" src="https://github.com/user-attachments/assets/b894ec92-64d7-4171-a273-90b4c9da7920" />

Создаем комментарий к индексу

Создаем комментарий к индексу и проверяем его  
<img width="811" height="146" alt="image" src="https://github.com/user-attachments/assets/a4a68cd0-a9f0-4a75-9ef3-c49e5e4a9846" />

***Реализовать индекс для полнотекстового поиска***  
Привет

***Реализовать индекс на часть таблицы или индекс на поле с функцией***  
Привет

***Создать индекс на несколько полей***  
Привет

***Написать комментарии к каждому из индексов***  
Комментарии к индексам создавались выше 


Структура


   CREATE OR REPLACE FUNCTION update_good_sum_mart()
    RETURNS TRIGGER AS $$
    
CREATE TABLE employees (
     id SERIAL PRIMARY KEY,
     first_name VARCHAR(50),
     last_name VARCHAR(50),
     department VARCHAR(50),
    salary NUMERIC(10, 2),
    hire_date DATE,
    bio TEXT
);

INSERT INTO employees (first_name, last_name, department, salary, hire_date, bio)
SELECT
    'Name' || g,
    'Surname' || g,
    CASE WHEN g % 3 = 0 THEN 'IT'
         WHEN g % 3 = 1 THEN 'HR'
         ELSE 'Finance' END,
    50000 + (random() * 50000),
    CURRENT_DATE - (random() * 3650)::int,
    'Biography of employee ' || g || '. Lorem ipsum dolor sit amet...'
FROM generate_series(1, 10000) g;

CREATE INDEX idx_employees_last_name ON employees (last_name);
