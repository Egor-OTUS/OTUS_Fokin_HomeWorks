**Стек**  
Virtual Box 7.2.4      
Ubuntu Server 25.10  
Характеристики ВМ    
RAM: 16GB    
CPU: 8 cores  
VDI(SSD): 200GB  
PostgreSQL 17  

Созданы 2 БД  

 *CREATE TABLE users (  
    id SERIAL PRIMARY KEY,  
    name VARCHAR(100),  
    email VARCHAR(100)  
);*

*INSERT INTO users (name, email)  
SELECT  
    'User' || number,  
    'user' || number || '@example.com'  
FROM generate_series(1, 100) AS number;*  

   *CREATE TABLE store (  
    id SERIAL PRIMARY KEY,  
    item VARCHAR(100),  
    amount VARCHAR(100)  
);*

*INSERT INTO store (item, amount)  
SELECT  
    'item' || number,  
    'amount' || number  
FROM generate_series(1, 100) AS number;*  

***Создать индекс к какой-либо из таблиц вашей БД***  
<img width="459" height="33" alt="image" src="https://github.com/user-attachments/assets/aaebabd8-9e7f-40ef-95b0-c80a9f908f14" />

Проверям создание индекса  
<img width="737" height="174" alt="image" src="https://github.com/user-attachments/assets/feb89b01-8120-4236-a8f6-1204961dffd0" />

***Прислать текстом результат команды explain, в которой используется данный индекс***  

Индекс не используется после analyze, т.к оптимизатор считает, что без него будет быстрее
<img width="589" height="104" alt="image" src="https://github.com/user-attachments/assets/3037e274-5725-4c13-9aa2-99cdb9de8055" />

 *Seq Scan on users  (cost=0.00..2.25 rows=1 width=28)  
   Filter: ((email)::text = 'user1@example.com'::text)  
(2 rows)*

Отключим временно Seq Sсan чтобы проверить будет ли использоваться наш индекс, теперь при запросах будет использоваться именно он, также мы видим, что "cost" запроса значительно увеличился 
<img width="630" height="137" alt="image" src="https://github.com/user-attachments/assets/d4978622-b5ba-4664-bbd1-77232ac49b08" />

 *Index Scan using idx_users_email on users  (cost=0.14..8.16 rows=1 width=28)  
   Index Cond: ((email)::text = 'user1@example.com'::text)  
(2 rows)*  

Создаем комментарий к индексу  


***Реализовать индекс для полнотекстового поиска***  
Привет

***Реализовать индекс на часть таблицы или индекс на поле с функцией***  
Привет

***Создать индекс на несколько полей***  
Привет

***Написать комментарии к каждому из индексов***  
Комментарии к индексам создавались выше 
